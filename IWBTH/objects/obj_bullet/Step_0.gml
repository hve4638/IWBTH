if tilemap_get_at_pixel(tile_collision, x + hspd, y)
    instance_destroy();

//var d = min(point_distance(x, y, xstart, ystart), 15);
			
//sprite_set_size(d, 1);

//image_xscale = max(image_xscale + 0.1, 3.6);
// image_alpha = 0.6;

x += hspd;