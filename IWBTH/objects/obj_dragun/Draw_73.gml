
/*var tx, ty
tx = 78;
ty = 20;

var ins = dragun[? DragunParts.hand_l];
var xx = ins.x + (tx - 44) * ins.image_xscale;
var yy = ins.y + (ty - 46) * ins.image_yscale;
draw_set_color(c_green);
draw_line(ins.x, ins.y, xx, yy);


var ins = dragun[? DragunParts.hand_r];
var xx = ins.x + (tx - 44) * ins.image_xscale;
var yy = ins.y + (ty - 46) * ins.image_yscale;

draw_line(ins.x, ins.y, xx, yy);

draw_set_reset();