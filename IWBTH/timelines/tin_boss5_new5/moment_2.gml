for(var i=0;i<t_n;i++) {
    if t_arr[i]
    with(sys_part){
        if ++prt[22]==0 event_user(0);
        part_emitter_region(Sys[22],emitter[22],(other.t_j+i+1)*64,(other.t_j+i+2)*64,608,616,1,0);
        part_emitter_burst(Sys[22],emitter[22],particle[22],20);
    }
}

scp_shake(1,2,0);
if t_i++ < 29 {
    timeline_position--;
} else {
    t_i=0;
}

