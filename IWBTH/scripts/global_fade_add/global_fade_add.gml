cout_show("global_fade_add() is nouse")/*

var add, col;
add = argument_count > 0 ? argument[0] : 0;
col = argument_count > 1 ? argument[1] : -1;

sys_global.fade_add = add;

if 0 <= col
	sys_global.fade_color = col;


